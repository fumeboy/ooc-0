---
name: library/http
type: how_to_use_tool
when: "当需要发送 HTTP 请求时"
description: HTTP/网关请求能力索引（子 trait 见本目录）
---

# library/http

本目录作为 `library/http/*` 子 trait 的索引入口。

## 子 Trait 列表

- `library/http/client`：HTTP 请求能力（GET/POST/通用请求）。
