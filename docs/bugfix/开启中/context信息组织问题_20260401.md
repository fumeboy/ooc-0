# Context 信息组织问题

> 日期：2026-04-01
> 状态：开启中
> 优先级：HIGH
> 关联 Flow：task_20260401032449_ud6b

---

## 问题描述

在执行"分析飞书文档"任务时，Agent 频繁犯语法错误，虽然最终能自我修复，但浪费了大量 tokens 和时间。

**错误统计（来自 process.json：

| 错误类型 | 出现次数 | 具体表现 |
|---------|---------|---------|
| `[/program]` 标记放错位置 | **7 次 | Agent 手动添加了 `[/program]` 结束标记 |
| `--params` 格式错误 | 2 次 | `--params token=xxx` 而非 JSON 格式 |
| `activateTrait()` 函数不存在 | 1 次 | 尝试调用不存在的函数 |
| `local.` 访问方式 | 1 次 | 混淆了 `local.` 与 `setData()` |

---

## 根本原因分析

### 问题 1：`[program]` 与 `[cognize_stack_frame_push]` 标记规则混淆

**在 llm.input.txt 中：

**`[cognize_stack_frame_push]` 的示例：

```
[cognize_stack_frame_push.title]
获取文档内容
[/cognize_stack_frame_push.title]

[cognize_stack_frame_push.traits]
lark-wiki
[/cognize_stack_frame_push.traits]

[/cognize_stack_frame_push]
```

**`[program]` 的示例：

```
注意，实际输出时，不需要在 [program] 段落前后添加三个反引号。
``` 
[program]
const file = await readFile("kernel/src/server/config.ts");
print(file);
```

**问题：**

| 标记类型 | 开始标记 | 结束标记 | 位置 |
|---------|----------|---------|
| 栈帧操作 | `[cognize_stack_frame_push]` | `[/cognize_stack_frame_push]` | ✅ 有 |
| 程序执行 | `[program]` | （无）| ❌ 没有 `[/program]` |

**Agent 的困惑：**

1. `[cognize_stack_frame_push]` 有对应的结束标记
2. `[program]` 也是"段落标记"
3. 所以 Agent 认为 `[program]` 也应该有 `[/program]` 结束标记
4. 结果：Agent 手动添加了 `[/program]`，导致解析错误

**证据（process.json 中的行号：**
- line 37-41：首次错误
- line 65-68：第二次
- line 91-94：第三次
- line 121-124：第四次
- line 135-138：第五次
- line 169-173：第六次
- line 177-181：第七次

---

### 问题 2：lark-wiki/lark-doc 只有一行描述

**llm.input.txt 中的 KNOWLEDGE 部分：

```
### Inactive (use activateTrait to enable)
...
- lark-wiki: 飞书知识库：管理知识空间和文档节点... (activateTrait to use)
- lark-doc: 飞书云文档：创建和编辑飞书文档... (activateTrait to use)
```

**问题：**

1. **只有一行描述，缺少：
   - 具体命令格式
   - 具体示例
   - 参数说明
   - `--params` 的正确格式

2. **Agent 只有在用户请求"分析飞书文档..."时，才需要这些信息，但 Context 中没有自动激活相关 trait 并注入其 readme。

**证据：

```
Agent 不得不手动读取 trait 的 readme：
- `readTrait("lark-wiki")` 返回 `[object Object]`
- `Bun.file(world_dir + "/library/traits/lark-wiki/readme.md").text()` 成功

这说明：
1. Context 应该在用户请求匹配关键词时，自动激活相关 trait
2. 并将其 readme 注入到 Context 中

---

### 问题 3：`activateTrait()` 函数与 `[cognize_stack_frame_push.traits]` 方式不同

**llm.input.txt 中的"认知栈控制 API" 部分：

```
认知栈控制 API
- `activateTrait(name)` — 为当前栈帧动态添加 trait
```

**但实际上：**

| 方式 | 是否存在 | 说明 |
|------|-----------|------|
| `activateTrait("lark-wiki")` | ❌ 不存在 | Context 文档说存在，但实际不存在 |
| `[cognize_stack_frame_push.traits]` | ✅ 存在 | 正确方式 |

**证据（process.json line 64-68）：

```
[program]
// 查看 lark-wiki trait 的完整内容
const larkWiki = await readTrait("lark-wiki");
...
// 激活 lark-wiki trait
activateTrait("lark-wiki");
```

结果：语法错误（因为 `activateTrait()` 不存在或行为与文档不符

---

### 问题 4：信息过载，关键信息被淹没

**llm.input.txt 的结构：

```
=== WHO AM I === (约 200+ 行
=== INSTRUCTIONS ===
  # 互联网访问能力 (约 50 行
  # 程序执行能力 (约 800+ 行！
  # 通信能力 (约 300+ 行
  # 验证能力 (约 150 行
  # 反思与经验沉淀能力 (约 200+ 行
  # 认知栈思维模式 (约 200+ 行
=== KNOWLEDGE ===
  # Trait 目录 (约 100+ 行
  # reporter trait (约 200+ 行
=== DIRECTORY === (约 20 行
=== PROCESS === (约 50 行
=== STATUS === (约 10 行
--- user --- (约 5 行
```

**问题：**

1. **INSTRUCTIONS 部分超过 1800+ 行
2. 很多信息对当前任务"分析飞书文档"是冗余的
3. 关键信息（lark-wiki/lark-doc 的具体用法）只有一行描述
4. Agent 很难在大量信息中找到关键信息

---

### 问题 5：缺少关键示例

**虽然有很多通用示例，但缺少：

1. **lark-cli 具体使用示例

错误方式：

```bash
lark-cli wiki spaces get_node --params '{"token": "xxx"}'
```

2. **`--params` 在 shell 脚本中正确传递 JSON 的示例

3. **`[program/shell]` 与 `#!/bin/sh` 关系说明

---

## 优化建议

### 建议 1：明确 `[program]` 不需要结束标记

**在 computable trait 的 readme 中增加：

```
## 段落标记规则

### 关键区别

`[program]` 是特殊的程序标记，与 `[cognize_stack_frame_push]` 等段落标记不同：

| 标记类型 | 开始标记 | 结束标记 | 说明 |
|---------|-----------|-----------|------|
| 段落标记（`[thought]` | 需要 | **需要** | `[thought]` 有 `[/thought]` |
| 程序标记（`[program]` | 需要 | **不需要** | 下一个段落标记或结束自动结束 |
| 栈帧操作（`[cognize_stack_frame_push]` | 需要 | **需要** | 有对应的结束标记 |

### 正确示例

```
[thought]
用户让我读取配置文件。

[program]
const content = await readFile("config.ts");
print(content);

[thought]
读取完成，准备回复用户。

[talk/user]
配置文件内容是...
[/talk]
```

注意：`[program]` 没有 `[/program]` 结束标记。下一个段落标记 `[thought]` 自动结束 `[program]` 段落。

### 错误示例

```
[program]
const content = await readFile("config.ts");
print(content);
[/program]  // ❌ 错误！不要添加 `[/program]` 结束标记会导致解析错误
```

---

### 建议 2：用户请求关键词匹配 trait 的 readme 自动注入 Context

**设计一个"智能 Context 组装器：

当用户请求包含以下关键词时：

| 关键词 | 自动激活的 trait |
|--------|------------------|
| "飞书文档"、"wiki"、"飞书"、"lark" | lark-wiki、lark-doc、lark-shared |
| "邮件"、"email" | lark-mail |
| "日历"、"会议" | lark-calendar |
| "任务"、"待办" | lark-task |
| "搜索"、"表格" | lark-doc、lark-sheets |

**注入方式：**

1. 解析用户消息中的关键词
2. 自动激活相关 trait
3. 将这些 trait 的 readme 注入到 Context 的 `=== RELEVANT TRAITS === 部分

**Context 中的新结构：**

```
=== RELEVANT TRAITS === (新增

## lark-wiki (已激活)

### 命令格式

```bash
lark-cli wiki <resource> <method> [flags]
```

### 快速开始

#### 1. 先查看 schema（推荐）

```bash
lark-cli schema wiki.spaces.get_node
```

#### 2. 执行命令

```bash
lark-cli wiki spaces get_node --params '{"token": "wiki_token"}'
```

### 重要：`--params` 参数必须是有效的 JSON 格式。

## lark-doc (已激活)

### +fetch shortcut（获取文档内容

```bash
lark-cli docs +fetch --doc <doc_token>
```

---

### 建议 3：统一 trait 激活方式说明

**在 Context 中明确：**

```
### Trait 激活方式

**推荐方式：通过栈帧属性**

```
[cognize_stack_frame_push.traits]
lark-wiki, lark-doc
[/cognize_stack_frame_push.traits]
```

**不推荐方式：activateTrait() 函数

`activateTrait()` 函数已废弃，请使用栈帧属性方式。

---

### 建议 4：Context 按需加载

**根据任务类型动态加载不同的 INSTRUCTIONS 部分：

| 任务类型 | 加载的内容 |
|---------|-------------|
| 飞书相关 | lark-wiki、lark-doc 的详细说明 |
| 代码修改 | 详细的 program 执行规则 |
| 通用任务 | 精简版 INSTRUCTIONS |

**或者：**

1. INSTRUCTIONS 部分只保留核心规则
2. 详细说明放在对应的 trait 的 readme 中
3. 根据用户请求关键词自动激活相关 trait 时，其 readme 被注入 Context

---

### 建议 5：增加关键示例

**在 lark-wiki/lark-doc 的 readme 中增加：

```
## 常见问题与示例

### Q: `--params` 格式错误

**错误示例：

```bash
lark-cli wiki spaces get_node --params token=xxx  # ❌ 错误！不是有效的 JSON
```

**正确示例：

```bash
lark-cli wiki spaces get_node --params '{"token": "xxx"}'  # ✅ 正确
```

### Q: 在 [program] 中如何执行 shell 命令

**方式一：使用 `exec()` 函数（推荐）

```
[program]
const result = await exec(`lark-cli wiki spaces get_node --params '{"token": "xxx"}'`);
print(result);
```

**方式二：使用 `[program/shell]` 段落

```
[program/shell]
lark-cli wiki spaces get_node --params '{"token": "xxx"}'
```

---

## 关联证据

### process.json 中的关键 actions：

| 行号 | action 内容 | 问题 |
|------|-----------|------|
| 32-34 | `[/program]` 标记放错 | 标记混淆 |
| 64-68 | `activateTrait()` 调用 | 函数不存在 |
| 121-124 | `--params token=xxx` | 格式错误 |
| 169-181 | `local.docMeta` | 访问方式错误 |

---

## 后续行动

- [ ] 检查 computable trait 的 `[program]` 标记规则说明
- [ ] 在 lark-wiki/lark-doc 的 readme 中增加命令示例
- [ ] 设计关键词匹配自动激活 trait 机制
- [ ] 验证改进后的效果（减少试错次数

---

---

## 详细错误模式分析

### 模式 A：`[/program]` 标记放错位置

**具体错误表现：

**错误 1（process.json line 37-41）：

```
[program]
// 读取 lark-wiki trait 的 readme 来了解正确用法
const larkWikiReadme = await readTrait("lark-wiki");
print(larkWikiReadme);
[/program]  // ❌ 错误！手动添加了结束标记
```

**错误 2（process.json line 65-68）：

```
[program]
// 查看 lark-wiki trait 的完整内容
const larkWiki = await readTrait("lark-wiki");
...
// 激活 lark-wiki trait
activateTrait("lark-wiki");
print("lark-wiki trait 已激活");
[/program]  // ❌ 错误！
```

**系统错误提示：

```
Syntax Error

[出错位置（第 ? 行）]
  1 | // 读取 lark-wiki trait 的 readme 来了解正确用法
  2 | const larkWikiReadme = await readTrait("lark-wiki");
  3 | print(larkWikiReadme);
  4 | [/program]
```

**Agent 的修复过程：

1. 第一次尝试：添加 `[/program]` → 失败
2. 自我反思：`"我看到问题了：上次的代码有语法错误（标记放错位置）"`
3. 第二次尝试：不添加结束标记 → 成功

**时间浪费：

| 步骤 | 思考轮次 | 说明 |
|------|-----------|------|
| 错误尝试 | 1 | 添加 `[/program]` 标记 |
| 错误提示 | 1 | 系统返回语法错误 |
| 自我反思 | 1 | 分析错误原因 |
| 重试 | 1 | 正确执行 |

**总计：至少 4 轮思考循环被浪费在这个错误上。

---

### 模式 B：`--params` 格式错误

**具体错误表现：

**错误 1（process.json line 121-124）：

```
[program/shell]
lark-cli wiki spaces get_node --params token=UbpdwXweyi86HHkRHCCcLPN4n8c
[/program]  // ❌ 错误！
```

**系统错误提示：

```
{
  "ok": false,
  "identity": "user",
  "error": {
    "type": "validation",
    "message": "--params invalid JSON format"
  }
}
```

**Agent 的修复过程：

1. 第一次尝试：`--params token=xxx` → 失败
2. 自我反思：`"我看到了问题：\n1. `--params` 需要的是 JSON 格式，而不是简单的 key=value\n2. 之前的代码中错误地包含了 `[/program]` 标记\n\n让我用正确的 JSON 格式调用。"`
3. 第二次尝试：`--params '{"token": "xxx"}'` → 成功

---

### 模式 C：`activateTrait()` 函数不存在

**Context 文档说：

```
认知栈控制 API
- `activateTrait(name)` — 为当前栈帧动态添加 trait
```

**Agent 尝试调用：

```
[program]
// 查看 lark-wiki trait 的完整内容
const larkWiki = await readTrait("lark-wiki");
...
// 激活 lark-wiki trait
activateTrait("lark-wiki");
print("lark-wiki trait 已激活");
[/program]
```

**结果：语法错误

**正确方式应该是：

```
[cognize_stack_frame_push.traits]
lark-wiki, lark-doc
[/cognize_stack_frame_push.traits]
```

---

## 时间与 Token 浪费统计

### 错误-修复迭代次数

| 错误类型 | 首次出现 | 最终修复 | 迭代次数 |
|---------|-----------|-----------|-----------|
| `[/program]` 标记 | act 4 | act 9 | **7 次错误尝试 |
| `--params` 格式 | act 14 | act 16 | **2 次错误尝试 |
| `activateTrait()` | act 7 | 未修复（改用其他方式 | **1 次 |

### 思考轮次浪费估计

**总 actions 数量：34 个（从 process.json 统计）

**有用的 actions：**
- 成功执行的命令：约 8 个
- 有用的思考：约 5 个

**浪费的 actions：**
- `[/program]` 标记错误：7 个
- `--params` 格式错误：2 个
- `activateTrait()` 错误：1 个
- `local.` 访问错误：1 个
- 其他试错：约 10 个

**估算：约 60% 的 actions 是错误尝试和自我修复。

---

## 模式根本原因深层分析

### 问题 1：标记一致性

**Context 中的不一致：

| 标记类型 | 开始 | 结束 | 示例 |
|---------|------|------|------|
| `[thought]` | 有 | 有 | `[thought]...[/thought]` |
| `[cognize_stack_frame_push]` | 有 | 有 | `[cognize_stack_frame_push]...[/cognize_stack_frame_push]` |
| `[talk/xxx]` | 有 | 有 | `[talk/user]...[/talk]` |
| `[program]` | 有 | **无** | `[program]...` （无结束标记 |

**Agent 的推理：

1. `[cognize_stack_frame_push]` 有 `[/cognize_stack_frame_push]`
2. `[talk/user]` 有 `[/talk]`
3. `[program]` 应该也有 `[/program]`
4. 所以我要添加 `[/program]`

**这是完全合理的推理！问题出在 Context 本身的不一致。

### 问题 2：示例优先级

**CLAUDE.md 原则：

> "示例 > 规则 — 给 LLM 看具体例子比抽象描述有效 10 倍

**实际情况：

| 内容 | 位置 | 详细程度 |
|------|------|-----------|
| `[program]` 规则说明 | computable trait | 规则描述 |
| `[program]` 正确示例 | computable trait | 有，但不够突出 |
| `[cognize_stack_frame_push]` 示例 | computable trait | 有，包含结束标记 |
| lark-wiki 命令示例 | **无** | - |
| `--params` 格式示例 | **无** | - |

**问题：

1. `[program]` 示例中没有明确说"不需要 `[/program]`"
2. lark-wiki/lark-doc 只有一行描述，没有示例
3. Agent 不得不通过试错学习

### 问题 3：函数文档准确性

**Context 中说：

```
认知栈控制 API
- `activateTrait(name)` — 为当前栈帧动态添加 trait
```

**但实际上：

1. `activateTrait()` 函数可能不存在
2. 正确方式是通过 `[cognize_stack_frame_push.traits]`
3. 这导致 Agent 困惑

---

## 相关文件

| 文件 | 问题 |
|------|------|
| `library/traits/computable/readme.md` | `[program]` 标记规则说明不够明确 |
| `library/traits/lark-wiki/readme.md` | 缺少具体命令示例 |
| `library/traits/lark-doc/readme.md` | 缺少具体命令示例 |
| Context 组装器 | 缺少关键词匹配自动激活 trait 机制 |

---

## 核心发现

**违反了 CLAUDE.md 原则：

> "示例 > 规则 — 给 LLM 看具体例子比抽象描述有效 10 倍

**问题是：

1. lark-wiki/lark-doc 只有一行描述
2. 缺少具体的命令示例
3. 缺少 `--params` 格式示例
4. `[program]` 标记规则与其他标记不一致

**结果：** Agent 不得不通过试错来学习正确方式，虽然能自我修复，但效率极低。

**估算：约 60% 的执行时间被浪费在错误尝试和自我修复上。