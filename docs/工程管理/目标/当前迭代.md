# 当前迭代

> 滚动更新。记录本周 / 本迭代的优先级。

## 当前优先级（2026-04-21 更新）

### 已完成

- ✅ Actions 压缩策略（P0）
- ✅ 经验沉淀（P1，Exp 012 完整验证通过）
- ✅ 线程树架构基本集成（P0，Bruce 体验测试通过）

### 进行中

- **P0: 文档重构**（本次任务）
  - 按 meta.md 概念树重建 docs/ 目录
  - 8 个 Phase：目录骨架 → 哲学 → 存在/结构 → 认知 → 合作/成长 → 人机交互 → 工程管理 → 修正 meta.md
  - 目前进入 Phase 7（工程管理）

- **P1: 线程树清理**
  - 旧 Router / Scheduler / ThinkLoop 清理（`kernel/src/flow/`, `kernel/src/process/`）
  - 保留开关 `OOC_THREAD_TREE=0` 可回退

### 待办

- **P2: 技术债**：flow.ts 已达 ~1175 行，需拆分（ExperienceManager, ProgramExecutor）
- **P2: meta.md 与代码对齐**（本重构后的 Phase 8）

## 更新规则

1. **一周一小结**：每周花 10 分钟整理当前优先级
2. **完成即迁移**：优先级完成后移到"已完成"区
3. **不超过 5 个 P0**：如果 P0 太多说明优先级不清，需要重新评估
4. **P0/P1 不可同时超 10 项**：把小项合并为 Epic

## 与看板的关系

日常追踪**在看板里**（Session 级的 Issue + Task）。这份"当前迭代"是**跨 Session 的汇总**——Supervisor 根据看板聚合而来。

典型流程：

```
日常工作：
  supervisor 用 session-kanban 管理 Issue + Task
  完成后 updateTaskStatus 标记

每周：
  supervisor 回顾所有 session 的完成情况
  更新 docs/工程管理/目标/当前迭代.md
  关联到 docs/工程管理/目标/现状.md 的月级更新
```

## 与基因的关联

- **G1**（数据即对象）— 这份文档也是对象数据
- **G10**（行动记录不可变）— 但每次更新都是覆盖式的（和 Kanban 的 comments 不同），因为它是"当前快照"，不是历史
